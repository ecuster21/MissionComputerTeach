{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "colcon build (Debug)",
      "type": "shell",
      "command": "bash",
      "args": [
        "-lc",
        "source /opt/ros/jazzy/setup.bash && colcon --log-base log/debug build --build-base build/debug --install-base install/debug --packages-select interfaces telemetry_telecommand --metas colcon.debug.meta --cmake-clean-cache"
      ],
      "options": {
        "cwd": "${workspaceFolder}/ros2_ws"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [
        "$gcc"
      ]
    }
  ]
}
